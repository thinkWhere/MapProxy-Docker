events {
    worker_connections  1024;
}

http {

    upstream mapproxy {
        server 127.0.0.1:8080;
    }
    server {

        listen 80;

        # For mapproxy
        location / {
            uwsgi_pass mapproxy;
        }
    }
}
